import { Typography } from "@mui/material";

export default function CheckoutPage() {
  return (
    <Typography variant="h3">
      This page should be visible to authorize person only
    </Typography>
  );
}
